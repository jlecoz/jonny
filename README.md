# Jonny

Jonathan Le Coz — Experiential Designer CV site (Next.js).

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Customize

Edit `config/siteConfig.js` for branding, theme colors, navigation, and contact details.
